{
    objects: import 'data://helm/apache?config-from=apache-config',
    config: {
        options: {
            repo: 'https://charts.bitnami.com/bitnami',
        },
        values: {
        }
    }
}
